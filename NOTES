COMPILATION

coq_makefile -no-install -I . *.v -o Makefile
make

coqdoc --latex -s -g --coqlib http://coq.inria.fr/stdlib/ -d coqdoc *.v


CODING

Be consistent in use of implicit arguments, notations, import vs export
Try making more arguments implicit (term F X etc)


NAMING

Singular CamelCase names for files and sections.


PROJECT

Compile with -R . LibraryName. Also, come up with a library name.

Use coqdoc.

Reintroduce install-doc, dist, and install-dist from the CoLoR Makefile.
